"""JOBS — the job-application capability (docs/plans/2026-09-15-job-application-agent.md).

Ships INERT: every tool here is denied by the policy gate until `agent_jobs_enabled` and the
matching tier switch are flipped. See `agent/loop/policy.py` § 3e.
"""
